// Header vazio - layout raiz já inclui TopNav
// Este arquivo existe apenas para compatibilidade com páginas antigas
export function Header({ isLoggedIn, user }: { isLoggedIn?: boolean; user?: any }) {
  return null;
}